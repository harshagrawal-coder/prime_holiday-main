GET Regions API — dropdown ke liye active regions fetch karo.
GET States API — table me database ki states show karo.
CREATE State API — modal se new state create karo.
UPDATE State API — same modal aur editingId reuse karo.
DELETE State API.
Toggle active/inactive API.
All / Active / Inactive filters.
Loading + error states.